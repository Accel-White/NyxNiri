# NyxNiri 主题同步问题总结

> **已过时** — 完整主题适配文档见 [`configs/noctalia/README.md`](configs/noctalia/README.md)。
> 本文件保留作历史调查记录，根因判断以新文档为准（本文档将 Nautilus 不跟
> 误判为 `prefer-dark-theme` 所致，真根因是 `gtk-4.0.css` 无条件 `@define-color`）。

> 调查日期：2026-08-24
> 状态：全部问题已解决

---

## 一、已解决的问题

### 1. `nyxniri theme toggle` 不广播 gsettings 信号

**根因**：`theme-sync.sh` 的 `toggle` 分支在调用 `noctalia msg theme-mode-toggle` 后直接 `exit 0`，跳过了 `set_system_theme`（gsettings/portal 广播）、INI 同步、kitty reload、kvantum 同步。

**影响**：`nyxniri theme toggle` 切换时，Chrome/Edge/Brave/Kitty 深浅色不跟随，只能靠 Noctalia hook 异步补上，延迟且不可靠。

**修复**：toggle 后 `sleep 0.3` → 读回新 mode → 继续跑完整 sync 流程（不再 exit 0）。

**验证**：`nyxniri theme toggle` 后 gsettings 立即变化，秒级跟随。

### 2. `theme-sync.sh` step 8 有害（config-reload + templates-apply）

**根因**：step 8 在 hook 触发时立即调用 `noctalia msg config-reload && noctalia msg templates-apply`。但 `theme_mode_changed` hook 在 Noctalia 调色板重算完成**之前**触发，templates-apply 用旧调色板渲染出错色。且 `config-reload` 会拖慢 Noctalia 调色板更新（从 ~6s 拖到 12-20s）。

**影响**：模式切换后 gtk.css 渲染错色（切 light 时仍为 dark 配色），且整体调色板更新延迟翻倍。

**修复**：移除 step 8。Noctalia 调色板更新后会自动渲染 user templates（实测 ~6s），无需手动触发。

**验证**：移除 step 8 后，gtk.css 在 ~6s 内自动更新为正确配色。

### 3. GTK Material You 模板未部署到 `~/.config/`

**根因**：工作树的 `noctalia-config.toml` GTK 注册块 + `configs/noctalia/templates/` 目录从未通过 `nyxniri install config` 部署到实机。`templates-apply` 不碰 gtk.css 是因为 toml 里没注册，不是缓存 bug。

**修复**：手动部署 toml + 模板目录到 `~/.config/`，注册后 `templates-apply` 完全正常（0.023s 重渲染，无占位符残留）。

**注意**：正式发布前需跑 `nyxniri install config` 让部署机制接管。

### 4. `atomic_update_ini` 未转义 regex 特殊字符

**修复**：key 经 `sed 's/[][\.^$*+?(){}|/]/\\&/g'` 转义后再用于 `[[ =~ ]]` 匹配。

---

## 二、未解决的问题

### 1. Nautilus（GTK4/libadwaita）深浅模式完全不跟随

**症状**：
- 切换 dark↔light 后，Nautilus 窗口一直保持 dark，不响应。
- 重启 Nautilus 后才能正确显示当前模式。
- 其他应用（Kitty、Brave "经典"模式等）秒级跟随正常。

**根因**：`theme-sync.sh` step 7 主动写入 `~/.config/gtk-4.0/settings.ini` 的 `gtk-application-prefer-dark-theme=true`，与 libadwaita 的 `AdwStyleManager:color-scheme`（读 portal color-scheme）互斥。libadwaita 官方明文警告此用法 unsupported，会导致 GTK4 应用视觉卡在 dark。

**修复**：
- theme-sync.sh step 7 不再向 gtk-4.0/settings.ini 写入 `gtk-application-prefer-dark-theme`（gtk-3.0 的保留）。
- theme-sync.sh 加一次性自愈：若 gtk-4.0/settings.ini 残留该 key，sed 删行。

**验证**：Nautilus 切换后秒跟，无需重启。

**复现范围**：
- Nautilus（GTK4 + libadwaita）❌
- gtk4-demo（GTK4 + libadwaita）❌
- 最小 Adw.ApplicationWindow 测试程序 ❌（视觉仍 dark，但 `notify::dark` 信号触发且值正确）
- Brave "经典"模式（GTK3）✅
- Kitty ✅

**已排除**：
- ❌ Portal 信号链路：`SettingChanged` 信号正确发出，`org.freedesktop.appearance color-scheme` uint32 值正确（0=no pref, 1=dark, 2=light）。
- ❌ gsettings 信号：`dconf watch` 确认 `color-scheme` 变化信号发出。
- ❌ Portal 后端选择：`XDG_CURRENT_DESKTOP=niri` 正确传播到所有 portal 进程。
- ❌ Portal 映射错误：`prefer-light→uint32 2`, `default→uint32 0`, `prefer-dark→uint32 1`，符合 xdg-desktop-portal 规范。
- ❌ adw-gtk3 主题包：已安装，GTK4 下 adw-gtk3-dark 和 adw-gtk3 内容相同（都只 `@import libadwaita.css`），`gtk-theme-name` 对 GTK4 无效。
- ❌ GTK_THEME 环境变量：未设置。
- ❌ dconf 服务：正常运行。
- ❌ libadwaita 版本：1.9.3，portal Settings ReadOne 支持（portal version=2）。

**主要嫌疑（未完全确认）**：

`~/.config/gtk-4.0/settings.ini` 中的 `gtk-application-prefer-dark-theme=true` 会触发 libadwaita 警告：
```
Adwaita-WARNING: Using GtkSettings:gtk-application-prefer-dark-theme with libadwaita is unsupported. Please use AdwStyleManager:color-scheme instead.
```

测试发现：
- 移除 `gtk-application-prefer-dark-theme` 后，最小测试程序的 `AdwStyleManager:notify::dark` 信号正确触发（dark→light→dark 值变化正确）。
- 但视觉上窗口仍然一直 dark（用户确认）——可能测试不够充分，或 libadwaita 的 `dark` 属性值虽变但 CSS 未重算/重绘。
- 移除该设置后 `notify::dark` 信号触发，说明该设置**确实干扰了 libadwaita 的信号处理**，但可能不是唯一的视觉问题原因。

**待验证**：
1. 移除 `gtk-application-prefer-dark-theme` 后，Nautilus 重启是否能正确跟随（需 `nyxniri install config` 部署修复后的 theme-sync.sh + 重启 Nautilus）。
2. libadwaita 的 `dark` 属性变化后，CSS 是否真的重绘（可能需要 `gtk4-widget-factory` + GTK_DEBUG=interactive 用 inspector 确认）。
3. 是否有其他 GTK4 CSS 覆盖（`~/.config/gtk-4.0/gtk.css` 的 M3 `@define-color`）干扰 libadwaita 的变量。

**niri 社区相关 issue**：
- [#2492](https://github.com/niri-wm/niri/issues/2492)：GTK4 apps started with `spawn-at-startup` not picking up system color-scheme。报告者用延迟启动解决，niri 作者认为是 portal dbus activation 竞争。但本机 gtk4-demo 从终端启动（portal 已就绪）仍不跟，不完全匹配。
- [#2878](https://github.com/niri-wm/niri/issues/2878)：Zen browser 主题问题，已关闭。

### 2. Noctalia 调色板更新延迟 ~6 秒

**症状**：模式切换后，gtk.css（M3 配色）约 6 秒后才更新。gsettings/portal 信号是立即的，所以深浅模式（Chrome/Edge）秒跟，但 M3 自定义颜色（Nautilus/Brave widget 配色）延迟 6 秒。

**根因**：Noctalia 调色板重算本身需要时间（壁纸 Material You 算法），这是 Noctalia 固有速度，NyxNiri 侧无法再快。

**结论**：可接受。深浅模式立即跟，M3 颜色 6 秒跟。

### 3. `gtk uninstall` 不持久

**根因**：GTK 模板注册写在源 `configs/noctalia/noctalia-config.toml` 中，每次 `nyxniri install` 会重新部署并自动重渲染。`nyxniri gtk uninstall` 从已部署的 toml 删除注册并删 gtk.css，但下次 install 会完全恢复。

**对比**：fcitx/greeter 在安装时动态写入 toml，卸载是持久的。

**结论**：核心特性定位下可接受，与 fcitx/greeter 行为不一致但不影响功能。

---

## 三、问题记录文档的错误假设纠正

原 `问题.md`（现 `notes/主题问题.md`）的多个结论经实机验证是错误的：

| 原问题.md 结论 | 实机真相 |
|---|---|
| 模板已注册部署 | **从未部署**——工作树 toml 改动 + templates 目录从未 sync 到 `~/.config/` |
| `templates-apply` 有缓存/不可靠 | 注册后**完全正常**，0.023s 重渲染，无缓存问题 |
| Noctalia 不自动渲染 user templates | **会自动渲染**（调色板更新后 ~6s）|
| `config-reload` 卡 30s 网络超时 | 实测 0.045s，无超时 |
| `gtk-dark.css` 软链接是根因 | 无关（GTK4 下 gtk-theme-name 无效）|
| `.rgb_csv` 渲染风险 | 实机验证成功，0 个占位符残留 |
| `palette-export --format gtk-css` 子命令 | **不存在**（`unknown command`），PR #20 的方案不可行 |

---

## 四、PR #20 对比

PR #20（已关闭，未合并）声称修复「全局主题切换同步 gsettings，Chrome/Edge 深浅色正确跟随」。

**PR #20 的方案**：
- toggle 不 exit 0（与本次修复一致）✅
- `noctalia msg palette-export --format gtk-css` 导出调色板到独立 CSS 并 `@import`（**该子命令不存在**，方案不可行）❌
- `atomic_update_ini` 转义（与本次修复一致）✅
- 壁纸选择器 Cairo→GTK 重构（PR 专属，NyxNiri 有自己的选择器，忽略）➖

**结论**：PR #20 唯一值得借鉴的是 toggle no-exit，其余 NyxNiri 已独立完成或方案不可行。

---

## 五、改动清单（已提交 commit 8f99f6e）

### `configs/noctalia/theme-sync.sh`
- toggle 不再 `exit 0`：sleep 0.3 + 读回 mode + 继续完整 sync。
- 移除 step 8（config-reload + templates-apply）：有害无益，Noctalia 会自动渲染。
- `atomic_update_ini` 转义 key 的 regex 特殊字符。
- dark/light 分支加 sleep 0.2。
- else 分支显式 `elif -n` 处理非空非 light scheme。

### `configs/noctalia/noctalia-config.toml`
- 新增 `[theme.templates.user.nyxniri_gtk3]` 和 `nyxniri_gtk4` 注册块。

### `configs/noctalia/templates/gtk-3.0.css` / `gtk-4.0.css`（新增）
- 移植自 HyprYou 的 M3 GTK 主题模板，由 Noctalia 渲染到 gtk.css。

### `nyxniri/gtktheme.py`（新增）
- `nyxniri gtk [install|status|uninstall]` 命令实现。

### `nyxniri/cli.py` / `doctor.py` / `i18n.py` / `backup.py` / `deploy.py`
- GTK 模块集成（CLI 注册、doctor 检查、i18n 文案、卸载清理、部署后触发渲染）。

### `tests/test_cli.py`
- GTK 模块退出码传播测试。

### `configs/noctalia/templates/gtk-3.0.css`
- 删除 GTK4 专有属性 `transform`（移植 bug）。

---

## 六、信号链路说明

模式切换时各应用的跟随机制：

```
nyxniri theme toggle / dark / light
        │
        ▼
theme-sync.sh
        │
        ├─ step 6: gsettings set color-scheme + gtk-theme-name  ← 立即广播
        │     │
        │     ├─ Chrome/Edge/Brave(经典): 读 gtk-theme-name → 秒跟 ✅
        │     ├─ Brave(GTK/QT 模式)/Firefox: 读 portal color-scheme → 秒跟 ✅
        │     ├─ Kitty: 不读 gsettings，靠 step 8 SIGUSR1 重载 ✅
        │     └─ Nautilus(GTK4/libadwaita): 读 portal color-scheme → ❌ 不跟（见未解决问题1）
        │
        ├─ step 7: atomic_update_ini settings.ini  ← GTK3 读，GTK4 启动时读
        │
        ├─ step 8: pkill SIGUSR1 kitty  ← Kitty 重载
        │
        └─ (Noctalia 自动，~6s)
              │
              └─ gtk.css 重渲染（M3 配色）→ GTK3 应用重载 CSS → M3 颜色跟
                                          → GTK4 应用不热重载 CSS → 需重启
```

**关键**：
- gsettings/portal 信号是**立即**的，深浅模式（明暗）秒跟。
- gtk.css（M3 自定义颜色）由 Noctalia **~6s** 自动渲染，GTK3 热重载，GTK4 需重启。
- Nautilus（libadwaita）深浅模式不跟是**独立 bug**，与 gtk.css 无关。
